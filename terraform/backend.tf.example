# Example: Configure remote backend for Terraform state
# Copy this to backend.tf and uncomment/configure for your use case

# Option 1: Azure Storage Backend (Recommended for Azure)
# terraform {
#   backend "azurerm" {
#     resource_group_name  = "rg-terraform-state"
#     storage_account_name = "terraformstate"
#     container_name       = "tfstate"
#     key                  = "sre-devops-portfolio.terraform.tfstate"
#   }
# }

# Option 2: Terraform Cloud Backend
# terraform {
#   cloud {
#     organization = "your-org-name"
#     workspaces {
#       name = "sre-devops-portfolio"
#     }
#   }
# }

# Option 3: S3 Backend (if using AWS)
# terraform {
#   backend "s3" {
#     bucket         = "your-terraform-state-bucket"
#     key            = "sre-devops-portfolio/terraform.tfstate"
#     region         = "us-east-1"
#     encrypt        = true
#     dynamodb_table = "terraform-state-lock"
#   }
# }

# Note: For this portfolio project, local backend is fine for single-user development
# Remote backend is recommended for team collaboration or production use

