apiVersion: v1
kind: ConfigMap
metadata:
  name: portfolio-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  portfolio-overview.json: |
$(cat k8s/grafana/dashboards/portfolio-overview.json | sed 's/^/    /')
