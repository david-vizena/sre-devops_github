apiVersion: v1
kind: Service
metadata:
  name: go-service
  namespace: default
  labels:
    app: go-service
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: go-service

